# ── Core image processing ──────────────────────────────────────────
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
scipy>=1.11.0
matplotlib>=3.7.0

# ── Ollama API client ──────────────────────────────────────────────
requests>=2.31.0

# ── Structured output validation ──────────────────────────────────
pydantic>=2.0.0

# ── Testing ───────────────────────────────────────────────────────
pytest>=7.4.0

# ── MolScribe (chemical structure → SMILES) ───────────────────────
# NOT included — requires torch<2.0 which conflicts with modern envs.
# The pipeline detects its absence and skips SMILES extraction gracefully.
# Architecture is in place (utils/molscribe_client.py) for future integration
# once MolScribe updates its torch dependency.
